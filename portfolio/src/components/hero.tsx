const Hero = () => {
  return (
    <section className="flex justify-center items-center min-h-screen w-full bg-gradient-to-r from-slate-400 to-slate-500 transition-all duration-500 dark:from-slate-950  dark:to-slate-900">
      Hero
    </section>
  );
};

export default Hero;
