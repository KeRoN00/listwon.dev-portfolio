const Hero = () => {
  return (
    <section className="flex justify-center items-center min-h-screen w-full bg-gradient-to-r from-orange-500 to-amber-500">
      Hero
    </section>
  );
};

export default Hero;
